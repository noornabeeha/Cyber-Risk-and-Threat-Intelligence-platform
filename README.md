create a .env file that contains your virus total API KEY:

.env file path must be: Cyber-Risk-and-Threat-Intelligence-platform/.env

VT_API_KEY = "yourApiKeyHere"

install all the requirements from requirements.txt by running:
pip install -r requirements.txt
